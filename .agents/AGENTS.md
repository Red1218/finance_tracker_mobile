# Agent Guidelines

Whenever you finish a task, produce a change manifest in the following format. That makes reviews faster because the user immediately knows whether they need to pay special attention to API changes or compatibility concerns.

```
Created
---------
...

Modified
---------
...

Deleted
---------
...

Public API Changes
------------------
...

Breaking Changes
----------------
None

Build Status
------------
✅ TypeScript
✅ Lint

Known Limitations
-----------------
None
```

---

# Expo Router Directory Boundary (ADR-011)

These rules are mandatory on all changes. Violations are architectural defects.

## Rule 1 — `app/` is for routes only

The `app/` directory at the project root must contain **only** Expo Router route
files: `_layout.tsx`, screen files (`.tsx`), and route group directories
(e.g., `(tabs)/`).

**Allowed in `app/`:**
- `_layout.tsx` (root and nested layouts)
- Screen files with a `default` export
- Route group directories: `(tabs)/`, `(auth)/`, etc.
- `+html.tsx`, `+not-found.tsx` (Expo Router special files)

**Never allowed in `app/`:**
- Providers, HOCs, guards, utilities, services, constants, types
- Any file without a default export

## Rule 2 — No directory named `app` anywhere under `src/`

Expo Router discovers route directories by path segment. A directory named `app`
anywhere in the project may be scanned as a route source.

**Never create:**
- `src/app/` or any subdirectory of it
- Any path matching `src/**/app/` at any depth

If a module group needs a home, name it after its role:
`bootstrap/`, `navigation/`, `providers/`, `platform/`, `core/`, etc.

## Rule 3 — Infrastructure modules belong in `src/`, never in `app/`

| Module type | Correct location |
|-------------|-----------------|
| App composition root | `src/bootstrap/` |
| Navigation guards and route constants | `src/navigation/` |
| Provider composition | `src/providers/` |
| Platform adapters | `src/platform/` |
| Core utilities | `src/core/` |
| Feature code | `src/features/<feature>/` |

## Rule 4 — `.expo/` is generated; never edit it manually

`.expo/types/router.d.ts` is auto-generated by Expo Router. It is gitignored
and must not be committed or edited by hand.

When route structure changes, regenerate it by running:

```powershell
Remove-Item -Recurse -Force .expo
npx expo start --clear
```

Never patch `.expo/types/router.d.ts` directly to fix TypeScript errors.
The correct fix is always to ensure the actual `app/` directory contains only
valid route files.

---

# Feature Definition of Done (DoD) & Merge Checklist

For every feature or bounded context (Accounts, Preferences, Categories, Budgets, Expenses, Analytics, etc.), ensure all of the following criteria are verified before merging:

- [ ] Architecture Review approved
- [ ] Domain Layer approved & frozen
- [ ] Application Layer approved & frozen
- [ ] Infrastructure Layer approved & frozen
- [ ] Presentation Layer approved & frozen
- [ ] Integration Layer approved & frozen
- [ ] Automated tests pass (`npm test`)
- [ ] TypeScript type checking passes (`npx tsc --noEmit`)
- [ ] Expo doctor checked (`npx expo-doctor`)
- [ ] Database schema & RLS policies verified against Supabase migrations
- [ ] ADR created/updated in `docs/adr/` and registered in `docs/adr/INDEX.md`
- [ ] No TODO/FIXME items remaining in feature codebase
- [ ] No architectural boundary violations introduced (Clean Architecture & ADR-011 enforced)
- [ ] Ready to merge
